## 2025-06-15 - [Wizard Navigation Accessibility]
**Learning:** Multi-step wizards should use semantic `<nav>` and `<ol>` structures with `aria-current="step"` and button elements instead of divs for keyboard and screen reader accessibility.
**Action:** Always wrap progress indicators in semantic navigation elements and use interactive elements (buttons/links) with proper ARIA labels.
